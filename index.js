const divide = () => {
    const result = 2000 / 100; // Calculate 2000 divided by 100
    console.log('divide result:', result); // Log the result to console for verification
    return result;
};
const square = (x) => {
    const result = x * x; // Calculate square of x
    console.log(`square of ${x}:`, result); // Log the result to console for verification
    return result;
}



    const add = (parameter1, parameter2) => parameter1 + parameter2;
add(a,b);
 //=> 7





